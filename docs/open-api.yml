openapi: 3.0.0
info:
  title: Music Webstore API
  description: API for managing a music webstore
  version: 1.0.0
servers:
  - url: http://REPLACE_WITH_CORRECT_URL
tags:
  - name: products
  - name: users
  - name: orders
  - name: order_lines
  - name: reviews
  - name: genres
  - name: products_genres
  - name: record_companies
  - name: artists
paths:
  /products:
    get:
      tags:
        - products
      summary: Get all products
      responses:
        '200':
          description:  Successfully got all the products
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Product'
    post:
      tags:
        - products
      summary: Create a new product
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProduct'
      responses:
        '201':
          description: Successfully created the product
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Product'
        '400':
          description: Could not create a new product, please check all fields are correct.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error400'

  /products/{id}:
    get:
      tags:
        - products
      summary: Get a specified product
      operationId: getProduct
      parameters:
        - name: id
          in: path
          description: The ID of the product to get
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successfully got the specified product
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Product'
        '404':
          description: No product with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'
    put:
      tags:
        - products
      summary: Update a product
      description: Update an existing product. Any combination of fields may be provided.
      operationId: updateProduct
      parameters:
        - name: id
          in: path
          description: The ID of the product to update
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProduct'
      responses:
        '200':
          description: Successfully updated the specified product
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Product'
        '400':
          description: Could not update the product, please check all fields are correct.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error400'
        '404':
          description: No product with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'
    delete:
      tags:
        - products
      summary: Delete a product
      description: Delete an existing product.
      operationId: deleteProduct
      parameters:
        - name: id
          in: path
          description: The ID of the product to delete
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successfully deleted the specified product
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Product'
        '404':
          description: No product with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'

  /users:
    get:
      tags:
        - users
      summary: Get all users
      responses:
        '200':
          description:  Successfully got all the users
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/User'
    post:
      tags:
        - users
      summary: Create a new user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUser'
      responses:
        '201':
          description: Successfully created the user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '400':
          description: Could not create a new user, please check all fields are correct.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error400'

  /users/{id}:
    get:
      tags:
        - users
      summary: Get a specified user
      operationId: getUser
      parameters:
        - name: id
          in: path
          description: The ID of the user to get
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successfully got the specified user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '404':
          description: No user with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'
    put:
      tags:
        - users
      summary: Update a user
      description: Update an existing user. Any combination of fields may be provided.
      operationId: updateUser
      parameters:
        - name: id
          in: path
          description: The ID of the user to update
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUser'
      responses:
        '200':
          description: Successfully updated the specified user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '400':
          description: Could not update the user, please check all fields are correct.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error400'
        '404':
          description: No user with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'
    delete:
      tags:
        - users
      summary: Delete a user
      description: Delete an existing user.
      operationId: deleteUser
      parameters:
        - name: id
          in: path
          description: The ID of the user to delete
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successfully deleted the specified user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '404':
          description: No user with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'

  /orders:
    get:
      tags:
        - orders
      summary: Get all orders
      responses:
        '200':
          description:  Successfully got all the orders
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Order'
    post:
      tags:
        - orders
      summary: Create a new order
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOrder'
      responses:
        '201':
          description: Successfully created the order
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'
        '400':
          description: Could not create a new order, please check all fields are correct.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error400'

  /orders/{id}:
    get:
      tags:
        - orders
      summary: Get a specified order
      operationId: getOrder
      parameters:
        - name: id
          in: path
          description: The ID of the order to get
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successfully got the specified order
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'
        '404':
          description: No order with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'
    put:
      tags:
        - orders
      summary: Update an order
      description: Update an existing order. Any combination of fields may be provided.
      operationId: updateOrder
      parameters:
        - name: id
          in: path
          description: The ID of the order to update
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateOrder'
      responses:
        '200':
          description: Successfully updated the specified order
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'
        '400':
          description: Could not update the order, please check all fields are correct.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error400'
        '404':
          description: No order with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'
    delete:
      tags:
        - orders
      summary: Delete an order
      description: Delete an existing order.
      operationId: deleteOrder
      parameters:
        - name: id
          in: path
          description: The ID of the order to delete
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successfully deleted the specified order
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'
        '404':
          description: No order with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'

  /orders/{order_id}/order_lines:
    get:
      tags:
        - order_lines
      summary: Get all order lines for a specified order
      operationId: getOrderLines
      parameters:
        - name: order_id
          in: path
          description: The ID of the order to get order lines for
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successfully got all order lines for the specified order
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/OrderLine'
        '404':
          description: No order with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'

  /reviews:
    get:
      tags:
        - reviews
      summary: Get all reviews
      responses:
        '200':
          description:  Successfully got all the reviews
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Review'
    post:
      tags:
        - reviews
      summary: Create a new review
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateReview'
      responses:
        '201':
          description: Successfully created the review
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Review'
        '400':
          description: Could not create a new review, please check all fields are correct.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error400'

  /reviews/{id}:
    get:
      tags:
        - reviews
      summary: Get a specified review
      operationId: getReview
      parameters:
        - name: id
          in: path
          description: The ID of the review to get
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successfully got the specified review
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Review'
        '404':
          description: No review with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'
    put:
      tags:
        - reviews
      summary: Update a review
      description: Update an existing review. Any combination of fields may be provided.
      operationId: updateReview
      parameters:
        - name: id
          in: path
          description: The ID of the review to update
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateReview'
      responses:
        '200':
          description: Successfully updated the specified review
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Review'
        '400':
          description: Could not update the review, please check all fields are correct.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error400'
        '404':
          description: No review with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'
    delete:
      tags:
        - reviews
      summary: Delete a review
      description: Delete an existing review.
      operationId: deleteReview
      parameters:
        - name: id
          in: path
          description: The ID of the review to delete
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successfully deleted the specified review
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Review'
        '404':
          description: No review with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'

  /genres:
    get:
      tags:
        - genres
      summary: Get all genres
      responses:
        '200':
          description:  Successfully got all the genres
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Genre'
    post:
      tags:
        - genres
      summary: Create a new genre
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateGenre'
      responses:
        '201':
          description: Successfully created the genre
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Genre'
        '400':
          description: Could not create a new genre, please check all fields are correct.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error400'

  /genres/{id}:
    get:
      tags:
        - genres
      summary: Get a specified genre
      operationId: getGenre
      parameters:
        - name: id
          in: path
          description: The ID of the genre to get
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successfully got the specified genre
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Genre'
        '404':
          description: No genre with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'
    put:
      tags:
        - genres
      summary: Update a genre
      description: Update an existing genre. Any combination of fields may be provided.
      operationId: updateGenre
      parameters:
        - name: id
          in: path
          description: The ID of the genre to update
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateGenre'
      responses:
        '200':
          description: Successfully updated the specified genre
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Genre'
        '400':
          description: Could not update the genre, please check all fields are correct.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error400'
        '404':
          description: No genre with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'
    delete:
      tags:
        - genres
      summary: Delete a genre
      description: Delete an existing genre.
      operationId: deleteGenre
      parameters:
        - name: id
          in: path
          description: The ID of the genre to delete
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successfully deleted the specified genre
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Genre'
        '404':
          description: No genre with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'

  /products_genres:
    get:
      tags:
        - products_genres
      summary: Get all product-genre associations
      responses:
        '200':
          description:  Successfully got all the product-genre associations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ProductGenre'
    post:
      tags:
        - products_genres
      summary: Create a new product-genre association
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProductGenre'
      responses:
        '201':
          description: Successfully created the product-genre association
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductGenre'
        '400':
          description: Could not create a new product-genre association, please check all fields are correct.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error400'

  /products_genres/{id}:
    get:
      tags:
        - products_genres
      summary: Get a specified product-genre association
      operationId: getProductGenre
      parameters:
        - name: id
          in: path
          description: The ID of the product-genre association to get
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successfully got the specified product-genre association
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductGenre'
        '404':
          description: No product-genre association with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'
    put:
      tags:
        - products_genres
      summary: Update a product-genre association
      description: Update an existing product-genre association. Any combination of fields may be provided.
      operationId: updateProductGenre
      parameters:
        - name: id
          in: path
          description: The ID of the product-genre association to update
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProductGenre'
      responses:
        '200':
          description: Successfully updated the specified product-genre association
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductGenre'
        '400':
          description: Could not update the product-genre association, please check all fields are correct.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error400'
        '404':
          description: No product-genre association with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'
    delete:
      tags:
        - products_genres
      summary: Delete a product-genre association
      description: Delete an existing product-genre association.
      operationId: deleteProductGenre
      parameters:
        - name: id
          in: path
          description: The ID of the product-genre association to delete
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successfully deleted the specified product-genre association
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductGenre'
        '404':
          description: No product-genre association with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'

  /record_companies:
    get:
      tags:
        - record_companies
      summary: Get all record companies
      responses:
        '200':
          description:  Successfully got all the record companies
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/RecordCompany'
    post:
      tags:
        - record_companies
      summary: Create a new record company
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRecordCompany'
      responses:
        '201':
          description: Successfully created the record company
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecordCompany'
        '400':
          description: Could not create a new record company, please check all fields are correct.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error400'

  /record_companies/{id}:
    get:
      tags:
        - record_companies
      summary: Get a specified record company
      operationId: getRecordCompany
      parameters:
        - name: id
          in: path
          description: The ID of the record company to get
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successfully got the specified record company
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecordCompany'
        '404':
          description: No record company with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'
    put:
      tags:
        - record_companies
      summary: Update a record company
      description: Update an existing record company. Any combination of fields may be provided.
      operationId: updateRecordCompany
      parameters:
        - name: id
          in: path
          description: The ID of the record company to update
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRecordCompany'
      responses:
        '200':
          description: Successfully updated the specified record company
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecordCompany'
        '400':
          description: Could not update the record company, please check all fields are correct.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error400'
        '404':
          description: No record company with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'
    delete:
      tags:
        - record_companies
      summary: Delete a record company
      description: Delete an existing record company.
      operationId: deleteRecordCompany
      parameters:
        - name: id
          in: path
          description: The ID of the record company to delete
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successfully deleted the specified record company
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecordCompany'
        '404':
          description: No record company with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'

  /artists:
    get:
      tags:
        - artists
      summary: Get all artists
      responses:
        '200':
          description:  Successfully got all the artists
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Artist'
    post:
      tags:
        - artists
      summary: Create a new artist
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateArtist'
      responses:
        '201':
          description: Successfully created the artist
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Artist'
        '400':
          description: Could not create a new artist, please check all fields are correct.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error400'

  /artists/{id}:
    get:
      tags:
        - artists
      summary: Get a specified artist
      operationId: getArtist
      parameters:
        - name: id
          in: path
          description: The ID of the artist to get
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successfully got the specified artist
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Artist'
        '404':
          description: No artist with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'
    put:
      tags:
        - artists
      summary: Update an artist
      description: Update an existing artist. Any combination of fields may be provided.
      operationId: updateArtist
      parameters:
        - name: id
          in: path
          description: The ID of the artist to update
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateArtist'
      responses:
        '200':
          description: Successfully updated the specified artist
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Artist'
        '400':
          description: Could not update the artist, please check all fields are correct.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error400'
        '404':
          description: No artist with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'
    delete:
      tags:
        - artists
      summary: Delete an artist
      description: Delete an existing artist.
      operationId: deleteArtist
      parameters:
        - name: id
          in: path
          description: The ID of the artist to delete
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successfully deleted the specified artist
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Artist'
        '404':
          description: No artist with that ID found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error404'

components:
  schemas:
    Product:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        description:
          type: string
        price:
          type: number
          format: float
        stock_quantity:
          type: integer
          format: int32
      required:
        - name
        - price
        - stock_quantity
    CreateProduct:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        price:
          type: number
          format: float
        stock_quantity:
          type: integer
          format: int32
      required:
        - name
        - price
        - stock_quantity
    UpdateProduct:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        price:
          type: number
          format: float
        stock_quantity:
          type: integer
          format: int32
    User:
      type: object
      properties:
        id:
          type: integer
          format: int64
        username:
          type: string
        email:
          type: string
        password:
          type: string
      required:
        - username
        - email
        - password
    CreateUser:
      type: object
      properties:
        username:
          type: string
        email:
          type: string
        password:
          type: string
      required:
        - username
        - email
        - password
    UpdateUser:
      type: object
      properties:
        username:
          type: string
        email:
          type: string
        password:
          type: string
    Order:
      type: object
      properties:
        id:
          type: integer
          format: int64
        user_id:
          type: integer
          format: int64
        total_price:
          type: number
          format: float
        order_date:
          type: string
          format: date-time
      required:
        - user_id
        - total_price
    CreateOrder:
      type: object
      properties:
        user_id:
          type: integer
          format: int64
        total_price:
          type: number
          format: float
    UpdateOrder:
      type: object
      properties:
        total_price:
          type: number
          format: float
    OrderLine:
      type: object
      properties:
        id:
          type: integer
          format: int64
        order_id:
          type: integer
          format: int64
        product_id:
          type: integer
          format: int64
        quantity:
          type: integer
          format: int32
      required:
        - order_id
        - product_id
        - quantity
    CreateOrderLine:
      type: object
      properties:
        order_id:
          type: integer
          format: int64
        product_id:
          type: integer
          format: int64
        quantity:
          type: integer
          format: int32
      required:
        - order_id
        - product_id
        - quantity
    UpdateOrderLine:
      type: object
      properties:
        quantity:
          type: integer
          format: int32
    Review:
      type: object
      properties:
        id:
          type: integer
          format: int64
        product_id:
          type: integer
          format: int64
        user_id:
          type: integer
          format: int64
        rating:
          type: number
          format: float
        comment:
          type: string
      required:
        - product_id
        - user_id
        - rating
    CreateReview:
      type: object
      properties:
        product_id:
          type: integer
          format: int64
        user_id:
          type: integer
          format: int64
        rating:
          type: number
          format: float
        comment:
          type: string
      required:
        - product_id
        - user_id
        - rating
    UpdateReview:
      type: object
      properties:
        rating:
          type: number
          format: float
        comment:
          type: string
    Genre:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
      required:
        - name
    CreateGenre:
      type: object
      properties:
        name:
          type: string
      required:
        - name
    UpdateGenre:
      type: object
      properties:
        name:
          type: string
    ProductGenre:
      type: object
      properties:
        id:
          type: integer
          format: int64
        product_id:
          type: integer
          format: int64
        genre_id:
          type: integer
          format: int64
      required:
        - product_id
        - genre_id
    CreateProductGenre:
      type: object
      properties:
        product_id:
          type: integer
          format: int64
        genre_id:
          type: integer
          format: int64
      required:
        - product_id
        - genre_id
    UpdateProductGenre:
      type: object
      properties:
        product_id:
          type: integer
          format: int64
        genre_id:
          type: integer
          format: int64
    RecordCompany:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
      required:
        - name
    CreateRecordCompany:
      type: object
      properties:
        name:
          type: string
      required:
        - name
    UpdateRecordCompany:
      type: object
      properties:
        name:
          type: string
    Artist:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        birth_date:
          type: string
          format: date
        record_company_id:
          type: integer
          format: int64
      required:
        - name
        - birth_date
        - record_company_id
    CreateArtist:
      type: object
      properties:
        name:
          type: string
        birth_date:
          type: string
          format: date
        record_company_id:
          type: integer
          format: int64
      required:
        - name
        - birth_date
        - record_company_id
    UpdateArtist:
      type: object
      properties:
        name:
          type: string
        birth_date:
          type: string
          format: date
        record_company_id:
          type: integer
          format: int64
    Error400:
      type: object
      properties:
        message:
          type: string
    Error404:
      type: object
      properties:
        message:
          type: string
